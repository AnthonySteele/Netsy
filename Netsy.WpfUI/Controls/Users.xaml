<UserControl x:Class="Netsy.WpfUI.Controls.Users"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ValueConverters="clr-namespace:Netsy.UI.ValueConverters;assembly=Netsy.UI"
    xmlns:Controls="clr-namespace:Netsy.WpfUI.Controls" >
    <UserControl.Resources>
        <ValueConverters:ShopColumnsValueConverter x:Key="ShopColumnsValueConverter" />
    </UserControl.Resources>
    <Grid>
        <ListBox 
                x:Name="usersListBox"
                ItemsSource="{Binding}" 
                ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="{Binding ElementName=usersListBox, Path=ActualWidth, Converter={StaticResource ShopColumnsValueConverter}}"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Controls:UserSmall />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
